# ABOUTME: Test dependencies template for hexagonal architecture migration
# ABOUTME: Add these to requirements.txt or create separate requirements-test.txt

# Core testing framework
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0

# Test utilities
faker==20.1.0                    # Generate fake test data
freezegun==1.4.0                 # Time mocking for time-based tests
pytest-env==1.1.3                # Environment variable management in tests

# Already in requirements.txt (verify versions)
# httpx==0.27.0                  # For FastAPI TestClient (already included)

# Optional but recommended
pytest-xdist==3.5.0              # Run tests in parallel with -n auto
pytest-timeout==2.2.0            # Timeout for long-running tests
pytest-randomly==3.15.0          # Randomize test order to catch dependencies

# Development tools (optional)
pytest-watch==4.2.0              # Auto-run tests on file changes
